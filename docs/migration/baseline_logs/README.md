# Baseline CLI Logs

Store before/after transcripts for automation flows here.

- `planner_claude.log` – snapshot of legacy Claude CLI behavior.
- `planner_codex.log` – snapshot after migrating to Codex CLI.
- Add reviewer/monitor variants as needed.

Use consistent prompts so regressions can be diffed.
