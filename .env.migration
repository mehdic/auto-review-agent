# LLM configuration for scripts/llm.sh
# Copy to .env or export values as needed.

# Primary CLI: codex (default) or claude
LLM_BIN=codex

# Optional model override (e.g., gpt-4.1, claude-3-opus)
LLM_MODEL=

# Extra flags passed to every invocation
LLM_ARGS=

# Presets that scripts automatically adopt when LLM_ARGS is empty
LLM_ARGS_CAUTIOUS=--output json
LLM_ARGS_AUTO=--auto-approve
